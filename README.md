# LeadStream

LeadStream is a lightweight lead management and streaming utility.

## Overview

This repository contains tooling to capture, process, and forward lead data.

## Getting started

1. Clone the repository
2. Install dependencies (see project files)
3. Run the application

## Notes

- `README.txt` remains as the original plain-text reference.
